<h1><%= @cocktail.name %></h1>
<% @cocktail.doses.each do |dose| %>

  <h2>Ingredients: <%= dose.description %> <%= dose.ingredient.name %></h2>
  <%= link_to 'Delete a dose', dose_path(dose), method: :delete %>
  <% end %>



<%= link_to 'Create a new dose', new_cocktail_dose_path(@cocktail) %>

